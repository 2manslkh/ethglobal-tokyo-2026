# Paper UI execution

Source of intent: approved UI improvement plan supplied by user; DESIGN.md is visual authority. Preserve backend contracts, auth/discovery gates, 5 rows × 4 columns and stored data.

## Assignments

- UI primitives, persistent presenters and all screens: GPT-6-Sol, high thinking, Herdr tab UI primitives and screens. Exclusive Assets/Tagtag/UI/** and newly imported Assets/Resources/Tagtag/UI/** plus Fonts resources. May add UI tests. No Core/AR/native edits.
- Camera presentation: GPT-6-Sol, high thinking, Herdr tab AR camera presentation. Exclusive Assets/Tagtag/AR/ArExperience.cs, new camera state helper and AR tests. No UI/Core/native map edits.
- Coordinator: shared Core interface, native MapKit presentation, build integration, documentation, Git index and Unity builds.
- Final review: GPT-6-Astra, high thinking (architecture/security and integration).

## Work stages

1. Adapt actual source primitives/typography/styles/icons/sheets/motion from ~/Desktop/coding-projects/sticker-app/unity. Copy necessary dependencies and real Bricolage Bold/ExtraBold and Instrument Regular/SemiBold fonts with OFL and provenance. No runtime source dependency. Paper/ink/yellow shared tokens, semantic states, internally styled fields/toggles, hidden stock scrollbars retaining scroll. 44+ point controls, focus, keyboard, disabled, helper/counter/validation behavior.
2. Persistent shell, separate screen presenters and overlay host. Update mounted controls; replace destination only on navigation. Preserve draft, selection, focus, scroll, book page, modal return focus. Remove focused-field update bypass. Three icon+label tabs; restrained yellow STICK, ink marker for selected Home/Explore.
3. Home sticker book leads with compact count, fixed 20 slots, no row lines/dots, gentle depth and warm paper; swipe plus previous/next, Taggi invitation Explore nearby, scroll small/large-text layouts. STICK camera leads, compact guidance and stable bottom actions. Four-pose picker sheet; Write note sheet with fields/limits/drafts/sign-in return/readiness/retry. Teaser-led discovery, artwork-led collection detail and restrained confirmation. Explore expands map, compact header/recenter and artwork/place/author/clue/Find in AR panel. Distinct loading/empty/location/failure with useful retry only. Account groups identity/authored/accessibility/management, equal Apple/Google actions; shared notices/destructive controls and keyboard-safe scrolling sheets.
4. Camera state separate from tracking/gates; typed CameraPresentationState on IArExperience with CameraPresentation property and existing Changed event. Inactive, Preparing, Live, PermissionDenied, Unavailable, Interrupted, Failed. UI opaque startup paper cover with Taggi and Getting the camera ready, revealing only on valid displayable frame. Reset on exit/interruption/resume. Opaque scene clear colors. Honest recovery and retry/settings. Source motion timings feedback120/state220/arrival320/exit200 and ease/spring; reduced motion, interruption cleanup, no animation-gated side effects, simple page transitions only.
5. Behavior tests: mounted continuity, drafts/focus, sheet dismissal, duplicate activation, camera transitions/first frame/interruption/tracking loss with imagery. Existing collection/map/service/AR/startup suite. Visual captures empty/populated, long note, large text, keyboard, errors/disabled and reduced motion. Unity EditMode and macOS player startup, separate ARKit preparation then iOS export and unsigned Xcode. Record actual physical camera startup/permission/tab/interruption/resume results or explicitly mark unverified.

## Coordination

Shared interface is CameraPresentationState IArExperience.CameraPresentation. UI consumes; AR implements; coordinator owns definition. Recovery retry can call Ar.Enter; iOS Settings via Application.OpenURL(app-settings:). No discovery gate changes.

All agents share /private/tmp/tagtag-paper-ui on feat/paper-ui. Do not run Unity concurrently. Send coordinator readiness for verification; coordinator runs tests and grants serial commit windows. Stage only owned files, inspect staged diff; atomic commits per coherent verified piece. Coordinator pushes integrated branch.

## Ledger

- Baseline main 2026-09-26 inspected; existing untracked TODO.md in original checkout preserved. Isolated Herdr worktree created. Impeccable context launcher unavailable, existing PRODUCT.md/DESIGN.md read directly.
- User plan already approves design; no new brainstorming or permission loop is needed.
- Shared interface preflight: UI/AR agree enum and property above. Native map lifecycle must preserve viewport while hidden, while UI controls overlay visibility.

- Test-first checkpoint: UI and camera behavior tests authored before helpers. Unity invocation /private/tmp/tagtag-paper-red.log stopped at CS0535 while ArExperience had not implemented the new interface; no behavior test result is claimed for that checkpoint.
- Backend regression: sandbox run could not bind localhost (EPERM); permitted rerun passed 18 tests, 1 emulator-only skip, 0 failures, /private/tmp/tagtag-paper-backend.log.
- Device availability: CoreDevice does not expose the physical phone, but ios-deploy detects Dawg. via USB (00008130-001420500E41001C). User will manually test once build is ready. Simulator iPhone 17 Pro iOS 26.2 is available.

- Shared components verification: 57/57 Edit Mode tests passed in isolated camera checkpoint with the stable pre-pass app view and new Paper components; /private/tmp/tagtag-primitives-tests.xml.
- Camera final checkpoint: 59/59 tests passed, including 9 camera lifecycle tests and permission/focus/pause ordering, /private/tmp/tagtag-camera-final-tests.xml. Camera files compared byte-for-byte with tested snapshot before commit window.
- Native MapKit Objective-C++ syntax check passed with the iPhoneOS SDK and exported Unity headers. Full Xcode build remains required.

- Stage commits: 19a8552 shared primitives/fonts plus enum; 41fa3f2 camera lifecycle plus interface; 829eb6c native viewport/artwork. The source UI presenter work remains separate until its mounted behavior passes.
- Review corrections while integrating: preserve actual field instances rather than rebuild/restore, include fields and switches in modal focus, avoid restarting active tab, reset presenter caches on navigation return, hide native map before modal presentation, convert keyboard pixels to panel coordinates.
- Final integrated Edit Mode checkpoint: 65/65 passed (`/private/tmp/tagtag-paper-edit-verified.xml`). Full Play Mode checkpoint: 2/2 passed (`/private/tmp/tagtag-paper-play-final.xml`), including actual startup and mounted UI assertions with 24 settled visual captures.
- Review repaired synced/revoked collection detail, clearing drafts after successful publication, abandoning sign-in return context, and restoring collection-sheet keyboard focus. Visual review repaired compact long-clue overflow with a scrollable teaser panel and gave both sign-in providers equal weight. Collection success invokes one artwork confirmation independent of persistence.
- Player integration found and fixed MonoBehaviour static Resources.Load initialization. Visual fixture now notifies after clearing busy and clears its render texture each frame; neutral gray in fake live-camera captures represents simulated imagery, not camera verification.
- Final residual focus case reproduced red when collection sync replaces the book cell; ID-based return-focus fix then passed full Play Mode 2/2 with 31 screen/sheet captures. Review findings closed. Screen commits: `0e3c913`, focused repair `258737b`; startup and integration tests `e6d68eb`.
- Both tests passed in the built macOS player before the bounded focus follow-up. Separate ARKit prepare/export, unsigned Xcode, and automatic signing passed. Updated Dawg. through USB without uninstalling; user notified that the build is ready for their offered camera recording/checks.
- Real simulator launch exposed a redundant initial status invitation beneath the empty book. The actual-scene assertion reproduced it, then passed after the bounded suppression fix. Final Edit Mode: 66/66. Commits `e1d3997` (presentation) and `29487ba` (actual startup assertion). Final device export and unsigned/signed compilation passed again; native map/keyboard interaction and physical camera video remain explicitly pending.
