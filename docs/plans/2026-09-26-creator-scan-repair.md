# Creator and scan repair execution

Approved: illustrated Upload/Photo/Imagine creator tiles; conditional messages; management in Home My designs; existing Polaroid flow; real four-stage AR scan feedback; yellow pencil-hatched detected surfaces; deploy design routes and isolate design errors; build and install on iPhone 15 Pro Max.

## Ownership

- creator-flow, GPT-6-Sol high, Herdr Creator flow: UI partials, UI tests and Application Play Mode tests. No Core/Services/AR edits.
- ar-scan, GPT-6-Sol high, Herdr AR scan feedback: AR, AR resources/tests, Core model contract. Add AppState.designError for service/UI consumers. Expose PlacementScanState ScanState on IArExperience; enum FindingSurface, SurfaceReady, Placed, Ready. Paused/tracking loss withdraw readiness; visibility of sheet must not change observational readiness. Update non-UI test doubles to compile.
- design-api, GPT-6-Sol high, Herdr Design API repair: Services and service tests, backend/deployment configuration and deployment notes. Isolate design failures in AppState.designError, including failure recovery consumers coordinated with UI agent. Deploy integrated API, required indexes, matching cleanup image; keep NFTs disabled.
- Coordinator: three Taggi raster assets under Resources/Tagtag/Creation, integration, all Unity runs and iPhone build/install. Git writes serialized after handoff; no agent commits until coordinator grants index ownership. Preserve TODO.md.

## Acceptance

Short creation labels below artwork; readable at compact/large text; pending-save recovery preserved; My designs closes creation and navigates to Home designs; native return remains creator. Four-stage state-driven indicator uses real tracking/anchor/map data without synthetic percentage. Stable plane-local translucent yellow pencil hatching, proper boundary clipping and lifecycle. Library/save errors do not leak into Write note. Verify affected Unity/backend suites, deployed authenticated design flow, then fresh signed install and focused device guide.

## Integration evidence

Coordinator executed cloud changes to avoid agent sandbox approval delays. API revision `tagtag-api-00003-75l` serves all traffic; both design indexes are ready and the cleanup job uses the identical image. NFT minting remains disabled. Synthetic authenticated verification stopped at IAM signing HTTP 403 before account creation; no IAM change was made. Production authenticated creation remains a device/manual check.

Initial combined verification: 174/174 Edit Mode, 21/21 graphics Play Mode, backend 68 passed/1 emulator-only skipped. A follow-up compact creator capture and complete PaperVisual suite passed 7/7. Captures and XML are in `docs/verification/creator-scan/`. Coordinator reviewed the creator, compact enlarged text, scan indicator, and hatch rendering captures.
